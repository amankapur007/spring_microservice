eureka:
  instance:
    hostname: localhost
  client:
    enable: true
    fetch-registry: true
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8761